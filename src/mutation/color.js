export function generateColorMutants(polynomial) {
	let mutantPolynomials = [];

    // ..apply all valid mutations

	return mutantPolynomials;
}

const colorMutations = [
	/*
    {
		name: string,
		isValid: () => boolean,
		execute: () => {
				        newValue,
				        appliedMutation: string
		            },
    },
    
    */
];
